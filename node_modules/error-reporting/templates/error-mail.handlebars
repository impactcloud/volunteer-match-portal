<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Errors</title>
</head>
<body>
    <small>Send time: {{time}}<br />Count: {{count}}</small>
    <h1>Errors occurred</h1>

    <div>
        {{#each errors}}
            <p>
                <small>{{this.time}}</small><br /><br />
                {{this.text}}
                {{#if this.err}}
                    <pre>{{this.err}}</pre>
                {{/if}}
                {{#if this.stack}}
                    <pre>{{this.stack}}</pre>
                {{/if}}
            </p>
            <hr>
        {{/each}}
    </div>

</body>
</html>
